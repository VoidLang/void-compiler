package org.voidlang.compiler.node.method.typed;

// TODO this will be used for lambda references
//   let foo = |x, y| x + 1
//              ^ here types are optionally added, but names are required
//   let sum = foo(1, 2)
public class TypedEntry {
}
